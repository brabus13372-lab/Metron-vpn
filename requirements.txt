# Telegram bot framework
aiogram==3.20.0

# Web API
fastapi>=0.115.0
uvicorn[standard]

# Async HTTP client — panel_client.py (3x-ui REST API)
aiohttp

# Async HTTP client — cleanup_orphan_keys.py
httpx>=0.27.0

# PostgreSQL async driver
asyncpg

# Pydantic v2 (FastAPI использует автоматически)
pydantic>=2.0.0

# Environment variables
python-dotenv

# Job scheduler (биллинг + уведомления)
APScheduler==3.11.0

# Required for Form(...) and UploadFile in FastAPI endpoints
python-multipart

# TTL cache (support.py rate limiting)
cachetools

# Timezone data для Docker/slim-образов (stdlib zoneinfo требует tzdata)
tzdata

# Test stack
pytest
pytest-asyncio
